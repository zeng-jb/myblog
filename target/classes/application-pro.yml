spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/myblog?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456

mybatis:
  type-aliases-package: com.zeng.pojo
  mapper-locations: classpath:mapper/*.xml   #ָ指定myBatis的核心配置文件与Mapper映射文件
  configuration:                              #驼峰映射
    map-underscore-to-camel-case: true

logging:
  level:
    root: info
    com.zeng: debug
  file:
    name: log/bolg-pro.log


server:
  port: 8080
